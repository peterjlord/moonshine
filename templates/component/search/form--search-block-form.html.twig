{# remove the wrapper block cause its not needed. #}
{%
  set remove_classes = [

  ]
%}
	
<form{{ attributes.setAttribute('role','search').removeAttribute('id').addClass('search-widget') }}>
  {{ children }}
</form>
